#include <bits/stdc++.h>
using namespace std;

#define ll long long

const ll MOD = 1e9 + 7;

ll grid[1005][1005];
ll dp[1005][1005];
int main() {
    memset(dp, 0, sizeof(dp));
    string s, t;
    cin >> s >> t;
    
}